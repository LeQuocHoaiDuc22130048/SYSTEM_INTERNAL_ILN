spring:
  application:
    name: import_export

  # Database (uncomment khi thêm dependency JPA + DB)
  # datasource:
  #   url: jdbc:mysql://localhost:3306/import_export_db
  #   username: root
  #   password: password
  #   driver-class-name: com.mysql.cj.jdbc.Driver
  # jpa:
  #   hibernate:
  #     ddl-auto: update
  #   show-sql: true
  #   properties:
  #     hibernate:
  #       dialect: org.hibernate.dialect.MySQLDialect

  # Profiles
  profiles:
    active: dev

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.suachuabientan: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
